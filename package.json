{
  "name": "middleware-auth",
  "version": "1.0.0",
  "description": "Express middleware example for logging and Bearer token authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
